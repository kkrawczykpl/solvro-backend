{
  "name": "solvro-backend",
  "version": "1.0.0",
  "description": "Recruitment task to join the Solvro scientific circle at the Wroclaw University of Science and Technology",
  "main": "dist/server.js",
  "scripts": {
    "prebuild": "rm -rf dist/* && tslint -c tslint.json -p    tsconfig.json --fix",
    "build": "tsc && cp -rf package.json dist/package.json",
    "prestart": "npm run build",
    "start": "ts-node .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/body-parser": "^1.19.0",
    "@types/express": "^4.17.8",
    "dotenv": "^8.2.0",
    "envalid": "^6.0.2",
    "mongoose": "^5.10.10",
    "ts-node": "^9.0.0",
    "tsc": "^1.20150623.0",
    "tslint": "^6.1.3",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "@types/mongoose": "^5.7.36",
    "body-parser": "^1.19.0",
    "express": "^4.17.1"
  }
}
